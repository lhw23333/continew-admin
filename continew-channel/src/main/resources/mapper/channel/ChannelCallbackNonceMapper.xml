<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="top.continew.admin.channel.adapter.ChannelCallbackNonceMapper">

    <insert id="claim" databaseId="mysql">
        INSERT IGNORE INTO biz_channel_callback_nonce
        (id, tenant_id, channel_code, product_code, config_version, callback_key_version,
         nonce_hash, received_time, expires_time, create_time)
        VALUES
        (#{id}, #{tenantId}, #{channelCode}, #{productCode}, #{configVersion}, #{callbackKeyVersion},
         #{nonceHash}, #{receivedTime}, #{expiresTime}, #{createTime})
    </insert>

    <insert id="claim" databaseId="pgsql">
        INSERT INTO biz_channel_callback_nonce
        (id, tenant_id, channel_code, product_code, config_version, callback_key_version,
         nonce_hash, received_time, expires_time, create_time)
        VALUES
        (#{id}, #{tenantId}, #{channelCode}, #{productCode}, #{configVersion}, #{callbackKeyVersion},
         #{nonceHash}, #{receivedTime}, #{expiresTime}, #{createTime})
        ON CONFLICT (tenant_id, channel_code, product_code, callback_key_version, nonce_hash) DO NOTHING
    </insert>
</mapper>
