spring:
  liquibase:
    enabled: true
    change-log: classpath:/db/changelog/db.changelog-postgresql.yaml

flowable:
  database-schema-update: false

mybatis-plus:
  configuration:
    database-id: pgsql
  extension:
    pagination:
      db-type: POSTGRE_SQL
